const john = 'john';
const peter = 'peter';

const sayHi = (name) => {
    console.log(`Hello there ${name}`);
}

sayHi('susan')
sayHi(john)
sayHi(peter)

//Modules - Encapsulated codes
//Every file is a module
